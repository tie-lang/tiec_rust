[package]
name = "tie-interp"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "tie 语言解释器：树遍历求值 AST，内置标准库"

[dependencies]
tie-frontend = { path = "../tie-frontend" }

[lints]
workspace = true
